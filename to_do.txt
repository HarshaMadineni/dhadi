      //store player 2 name in db
      //check if game code exists for player 2
      //delete table ajax request
      //check gcode while creating game
      //get opp move
delete non started games: get list of all tables, use for loop and delete
empty ones except 2; use IF (SELECT * FROM tablexy) DROP TABLE tablexy
don't play until opponent is online:
